name: tactanotes_ui
description: "Offline-First AI Notes & Learning Companion"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  shared_preferences: ^2.2.0
  google_fonts: ^6.1.0
  permission_handler: ^11.0.0
  path_provider: ^2.1.0
  flutter_rust_bridge: 2.11.1 # Pinned to match Cargo.toml and CLI
  file_picker: ^8.0.0
  image_picker: ^1.1.0
  signature: ^5.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_driver:
    sdk: flutter

flutter:
  uses-material-design: true
